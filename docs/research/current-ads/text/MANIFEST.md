# Current text ads manifest

Google's Ads Transparency Center identified the verified advertiser as
`Uprising Science Private Limited` with advertiser ID
`AR03970684002292989953`.

A seeded sample of 50 creative IDs has been selected from 308 discovered text
creative IDs. The locked selection is in `google-random-sample-50.json`; seed:
`20260911`. Retrieval and semantic deduplication of the selected copy is still
in progress.

The earlier partial extraction in `google-text-ads.json` is not the locked
sample and must not be presented as its result.

Google responsive-search combinations will be deduplicated by message and claim
structure, not counted as independent merely because Google recombines the same
headline and description assets.
